import { FC } from "react";
import "./styles.css";

const PlusIcon: FC = () => {
  return (
    <div className={"plusIcon"}>
      <span className={"plusSpan"}>+</span>
    </div>
  );
};

export default PlusIcon;
